# 給 Design 的畫布回饋（核實至 2026-09-01 第五版畫布）

> **這份文件的用途**：把**尚未解決**的畫布問題逐條列出，讓畫布可以更新到最新狀況。
>
> ⚠️ 這是**第二類**文件：與 `IMBRACE_QUESTIONS.md`（給 iMBrace）一樣，內容會離開這個 repo。
>
> **本檔只收未解項目。** 已核實「畫布改了、實作也跟上」的條目一律移除。
> **未列在這裡的一切，都以畫布為準。**

---

## A. 待處理項目

### A-1　情緒走勢圖漸層的 `stop-color` 寫成了 attribute，在瀏覽器裡會整組失效

第五版畫布的 `CopilotPanel` 走勢圖分帶漸層逐字是：

```html
<stop offset="0.2" stop-color="var(--info)"></stop>
```

`stop-color` 是 presentation attribute，而 **presentation attribute 不做 `var()` 代換** ——
`var(--info)` 在瀏覽器裡是無效值，無效的 `stop-color` 會**靜默退回黑色**。
不會有錯誤訊息，只會得到一條黑線；五個 stop 全部如此，整條折線變全黑。

畫布在自家渲染器裡看起來正常，所以這個問題只在**匯出成瀏覽器可開的 HTML 或用瀏覽器截圖**時才會現形。

**改法**（兩邊都有效）：

```html
<stop offset="0.2" style="stop-color: var(--info)"></stop>
```

⚠️ 我方實作已改用 `style` 寫法，功能不受影響；提出來是因為畫布本身的匯出物會壞掉。

---

2026-09-01 提出的另兩項已於同日的畫布更新中全數採納：

- **情緒走勢圖的基準線**由 `--border` 改為 `--border-strong` —— 原值疊在 `--surface-2` 上
  淺色只有 1.07:1、深色約 1.2:1，等於畫了一條看不見的線。
- **artboard 2a 示範資料的「近 5 輪」與軸標籤「第 50 輪」矛盾**已訂正為一致的 50，
  走勢摘要裡對不上的「第 4 輪」也一併改掉。

---

## B. 請勿回退

> 這一段**沒有待辦**。列出來是因為這些位置很容易在下一輪設計時被善意地「補回去」或「調回去」，
> 而改回去之後我們只能再拿掉一次。

### B-1　畫布畫得到、但平台資料拿不到

| 位置 | 現在的畫法（正確） | 為什麼不能放回原本的樣子 |
|---|---|---|
| 客服身分（頂列／presence／訊息泡泡） | **email**（`agent.lin@company.com`） | 平台沒有人名：登入回應沒有 `display_name`／`name`，團隊名冊 `display_name` 實測 12/12 全是 email。我們**不會**從 email 反推姓名——同名同姓、共用信箱、非英文名字都會產生錯的名字，而認錯同事正是撞單防護與 presence 最不能出錯的地方 |
| 左欄列項第二行 | 頻道 icon ＋ presence 標記 | 對話清單的 payload 沒有「最後一則訊息」的文字或發送者，要顯示就得對每一列各打一次訊息 API |
| 「N 則」數字 | 不帶總數的措辭 | 訊息 API 只回一頁 ＋ `hasMore`，沒有剩餘則數；組織清單也沒有進行中對話數 |
| 客戶輸入狀態 | **沒有這個元件** | 平台不提供。假造一個比沒有更糟——客服會為了等一個不存在的訊息而停手 |
| AI 訊息 meta | 只有時間（到秒） | 訊息 payload 沒有意圖與信心欄位；到秒是因為撞單判斷靠的正是秒級先後 |
| 未讀 | **圓點＋「未讀」** | 我們的新訊息訊號在一次輪詢間隔內來幾則都只跳一次，數出來是「批次數」不是「則數」 |
| 空狀態的「只看未回覆」 | **沒有這顆按鈕**（1d） | 清單 payload 沒有「最後一則是誰送的」，篩不出「未回覆」。⚠️ 本條 2026-09-01 才補登記 |
| OTP 錯誤的「還可嘗試 N 次」 | **不顯示次數**（1a） | 驗證 API 沒有剩餘次數欄位，數字只能用猜的。⚠️ 本條 2026-09-01 才補登記 |

### B-2　為了無障礙而定的兩個 token

| token | 決定 | 為什麼不能調回去 |
|---|---|---|
| `--open` | 淺色為 `#8a5d05`（曾為 `#a3700a`） | 舊值疊在 `--open-bg` 上只有 **3.87:1**，過不了 WCAG AA 內文的 4.5:1。這個 token 用在摘要過期提示、知識庫過期註記、`open` 狀態標籤、「挽留」語氣標籤 —— **每一處都是文字**，調亮會同時破線 |
| `--info` | 淺 `#20406f`／深 `#9dc0f2`，專供 `--navy-soft` 底上的文字 | ⚠️ **不要改成直接調亮 `--navy-2`** —— 它同時是按鈕的 hover 底色，調亮會讓按鈕上的白字失去對比。分成兩個 token 才能各自最佳化 |

> 五種語氣標籤在兩個主題下的對比我方都實算過，十組全部通過 AA（最低 5.17:1）。

---

## C. 實作刻意與畫布不同（告知，不必改畫布）

> 這些是我方**已裁示**的選擇，列出來是為了讓下一輪核對時不會被當成「實作沒跟上」。
> ⚠️ 編號留缺不遞補，以免與先前轉出的版本對不上。
>
> ✅ **2026-09-01 第三／四版畫布採納了先前 C 段的絕大多數**（字級加大並改 `rem`、
> 情緒走勢圖整欄寬、摘要的可展開「詳細內容」、「產生於」、「重新產生」只在失敗時可按、
> header 不顯示寬度數字、建議卡捲動高度 480px、服務模式說明多一句、拖曳把手的鍵盤操作、
> 頭像下拉的 email 換行、中欄標題列的「已載入 N 則」）。那些條目已全數移除。

| # | 項目 | 畫布 | 實作 | 理由 |
|---|---|---|---|---|
| **C-19** | **情緒示警 pill 的文案** | 逐字「焦慮偏高」 | 「客戶情緒：挫折」／「客戶情緒：生氣」 | 我方是**五級分類**（平靜／普通／擔憂／挫折／生氣），畫布只示範了其中一種情況的措辭。規格（001 FR-003）要求「挫折」與「生氣」在文字上就能互相區分 |
| **C-20** | **五段量表的「生氣」配色** | 反白的 `--warn`（與「挫折」同色系） | `--danger` 系 | 同上：兩級共用 `--warn` 就分不出來了。⚠️ `--danger` 這組 token 已是畫布定義的值，不是我方自訂 |
| **C-21** | **五段量表的標籤語言** | `calm`／`neutral`／… | **中文**（平靜／普通／擔憂／挫折／生氣） | 與語氣標籤同一個方向：面板是給客服看的即時輔助，不是給工程師看的列舉值 |
| **C-22** | **走勢摘要的強調** | 整段同一色、無強調 | 後半的「建議…」加粗 | 客服真正要用的是那一句；走勢那半在折線圖上已經看得到 |
| **C-23** | **走勢圖的 `score` 刻度** | `score 0.72`（0–1） | `score 72`（0–100） | 我方 `SentimentPoint.score` 的定義就是 0–100。換算成 0.72 會讓畫面與 API／日誌變成兩套數字 |
| **C-24** | **走勢圖的輪數（tag 與軸標籤右端）** | 逐字「近 50 輪」／「第 50 輪」 | **這張圖實際畫了幾輪**，兩處同一個數字 | 50 是視窗上限，不是真的畫了幾輪。⚠️ 實務上永遠到不了 50：分析輸入是最新 50 **則訊息**，其中只有客戶的文字訊息會變成評分點，一來一往的對話約佔一半 → 天花板約 25 |
| **C-25** | **走勢圖的筆畫縮放** | `stroke-width:2`（畫板固定 420px） | 加 `vector-effect="non-scaling-stroke"` | 畫布的畫板寬度固定，我方的面板**可拖曳 320–720px**。等比 viewBox 之下筆畫會跟著放大，720px 時那條 2 的線會被畫成約 4px。加上之後幾何仍完全等比、線寬固定為畫布給的值 |
| **C-27** | **左欄列項的頭像配色** | 一律 `--surface-3` 單一灰 | **六色輪替**（依代號雜湊） | 讓客服在長清單裡能用顏色快速定位同一個對話。⚠️ **但訊息泡泡裡的客戶頭像維持畫布的單一灰**：色盤裡有一組是 `--ai-bg`／`--ai`，而在訊息流這個畫面裡 `--ai` 是語意色（「這則是 AI 發的」），用它畫客戶頭像等於讓同一個顏色講兩件相反的事。畫布自己的示範代號 `TWN#GW4772` 雜湊後正好落在那一組 |
| **C-28** | **左欄「載入更多對話」** | spinner 自動載入列 | **可按的按鈕** | 自動載入會在客服往下捲時持續打 API 且無法中止；載入時機交給人決定 |
| **C-29** | **左欄搜尋列** | 只有搜尋框 ＋ 收合鈕 | 多一顆重新整理鈕 | 輪詢有間隔，客服偶爾需要立刻確認清單是最新的 |
| **C-30** | **未接手時的輸入區** | 虛線提示框取代整個輸入區 | **只有「尚未接手」這一種**照畫布 | 另外兩種不能送出的情況（Automation Only 唯讀、同事鎖）維持「說明框 ＋ 停用的輸入框」：那兩者是**暫時**狀態，客服可能想先把草稿打好，而畫布沒有涵蓋它們 |
| **C-31** | **連續訊息的分組條件** | （畫布只示範連續兩則 AI） | 只看**發送者**，不看時間間隔 | 平台的訊息時間戳在同一批可能相同或極接近，加上「N 分鐘內才算同一組」會產生不穩定的分組。⚠️ 真人客服另比 `sender.id`——只看 `type` 會把兩位同事的連續發言併成一組，第二位的 email 就此消失 |
| **C-32** | **輸入框高度的下限** | 兩行 `72px`（副標逐字「兩行 72px – 320px」） | 一行 **`46px`**（預設仍是 `72`） | 小螢幕筆電上，72px 的下限會把訊息流壓到看不到幾則，而客服真正在讀的是訊息流。46 ＝ 一行 `16px × 1.6` ＋ 上下 padding 各 10px。⚠️ 原本訂在兩行的理由（「再低會讓 Shift+Enter 換行看起來像壞掉」）改由 `rows="1"` 承擔 —— 一行是誠實的一行，不是被切一半的兩行。⚠️ **只動下限，不動預設**：多數螢幕上兩行仍是對的起點，高度會存進 `localStorage`，需要的人拖一次就記住 |
