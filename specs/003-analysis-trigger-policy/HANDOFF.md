# 交接筆記 —— M2 收尾（002／003／004）

> ⚠️ **有時效的滾動快照。做完一段就把該段刪掉**，不要讓它變成第四個描述同一件事的地方
> （`CLAUDE.md` 的核心警告）。正典結論都在各自的 spec、`docs/ARCHITECTURE.md`、
> `docs/CONSTITUTION.md`、`docs/IMBRACE_QUESTIONS.md` —— **這裡只指路，不重述**。
>
> **最後更新：2026-08-29**

---

## 1. 現況

分支 `feat/m2-copilot-panel`，**未押任何 tag**。`origin` 與本地 HEAD 同步於 `3070f8d`。

| 規格 | 狀態 |
|---|---|
| 001 | 完成，`m2-001-done` 已押 |
| 002 | implement 完成（32/32）。**完整人工驗收未做**，SC-001 真實環境計時從未執行 |
| 003 | implement 完成，**54/55**。唯一未完成是 T052 的 US2-C（見下） |
| 004 | 只有 spec + checklists（16/16 全勾、0 筆待釐清），**plan 未跑 → 這是下一步** |

**2026-08-28 夜間完成 003 的真實環境驗收**：SC-001、SC-002、T037（US3 AC#1／AC#2）、
T052 的 US1-A／US2-B／US2-E／T032a 全數通過。數據與判讀方式在 `quickstart.md` 與
`docs/ARCHITECTURE.md` 的 M2 驗收清單，**本檔不重述**。

過程中挖出**四個新缺陷**，全部已登記於 `docs/ARCHITECTURE.md` M2 驗收清單的未打勾工項
（`runColdStart()` 重啟復原缺口、`SentimentGauge` 1 點空白框、自動恢復不補算失敗批次、
`session.watchers` 雙分頁缺陷）。**已決議留到 004 之後開 005，不阻塞 004。**

⚠️ **有一個會反覆絆到你的**：伺服器重啟後，已 JOIN 的對話面板會**永久空白、不報錯**
（`runColdStart()` 只由 `join.post.ts` 呼叫，而平台側 JOIN 是持久的）。
**每次重啟 dev server 後都要 LEAVE 再 JOIN 才會重新分析** —— 當晚撞到四次。

---

## 2. 未解決事項

### 卡在外部條件

- **T052 US2-C** —— ⛔ 需要**兩位不同的客服帳號**（真實登入是 email + OTP），目前只有一組。
  ⚠️ **MUST NOT 以同一帳號兩分頁替代** —— 那是 T032a 的情境，驗的是不同的東西。
  連帶使 FR-016a（伺服器端不推分析事件給未 JOIN 的連線）在真實環境無證據，
  目前只有 `test/stream-analysis-visibility.test.ts` 與 `smoke:realtime` 的自動化涵蓋。
- **002 的 SC-001（3 秒／10 秒真實環境計時）從未執行過**（002 `tasks.md` T069）。
  ⚠️ **MUST 排在 004 之後** —— 004 的 FR-001 改的正是那個時限的達成方式。

### 待裁決

- **FR-014 的 15 秒單次逾時門檻是否放寬** —— ⚠️ **2026-08-29 後約束方換人了。**
  原本的理由是摘要 agent 曾出現 42.9 秒尖峰，但重量 n=15 最慢只有 7646ms（餘裕 49%），
  該理由**目前沒有新證據支持**（也不足以認定尖峰消失——那次是真實發生過的）。
  現在最吃緊的是**建議卡第二段：最慢 13032ms，餘裕僅 13%**。
  而第二段的延遲直接受 004 自己的設計影響，**所以這題 MUST 在 004 設計定案後才裁決**。
  四個 agent 的完整基準表在 `ARCHITECTURE.md` §8.2b。
- **~~`callAgent()` 是否傳 `user_id`~~ → 已量測，建議做，但不是效能解方。**
  實測中位 **54ms**、id 20/20 穩定可快取、傳入後輸出 5/5 正常。
  ⛔ 但它**補不上 FR-001 的 310ms 缺口**（只有 17%）——我先前把它列為「可能翻轉判定」
  的槓桿是推測錯誤。它是**衛生問題**：省一趟往返、型別本來就宣告了該欄位。
  實作與否可併入 004 或另立，不阻塞任何事。

### 已決定，不要再重新討論

- **雙分頁缺陷（`registerCredential()`／`session.watchers`）不現在開 spec** —— 004 之後開 005。
- **不改 `SENTIMENT_CHUNK_SIZE`（維持 6）** —— 已用實測排除：固定成本 7～8 秒、每則邊際
  僅約 0.5 秒，且當晚實際逾時的批次只有 1～2 則，**卡住的不是批次大小**。
- **`/speckit-implement` 沒有 commit 步驟，維持現狀** —— 跑 implement 時自己記得每個 phase
  收尾用 `/commit-split`。
- **SC-007 依決定結案，而非依判準通過** —— 原判準的「未參與者讀文案」測試從未執行。
  理由與回收條件記在 `tasks.md` 該子項與 `ARCHITECTURE.md` 的 M3 驗收清單。
- **憲法 5.3 待修憲**（主鍵改 uuid ＋ 草稿 id 為冪等鍵）已承接至 `CONSTITUTION.md`，
  期限「**M3 開工前**」、MAJOR 變更。**不阻塞 004。**

---

## 3. 下一步

1. ✅ **004 plan 需要的量測已於 2026-08-29 全部做完。**
   數據在 `docs/ARCHITECTURE.md` §8.2b（模型比較、四個 agent 基準表、`user_id` 往返）
   與 `agent-knowledge-provider.ts` 的常數註解（知識庫檢索），**本檔不重述**。結論：

   - **模型維持 `google.gemma-3-27b-it`，已換回，不必再議。** n=15 下 gemma 與 gpt-oss
     中位數只差 8ms，但 gemma 變異度小 3.6 倍、第二段引用覆蓋 15/15 對 3/5。
   - **兩段式成立**：檢索中位 11.9 秒且 **0/12 落在 8 秒內**，第二段端到端約 22 秒
     （檢索 11.9 ＋ 生成 10.0，中位）對第一段 9.2 秒。
   - **⛔ `SUGGESTION_RETRIEVAL_TIMEOUT_MS`（8 秒）必須改**，否則 004 的第二段永遠不會觸發
     （0/12）。新值應參考檢索 p90 = 16.9 秒。這是 plan 的必辦項，不是選項。
   - **⚠️ FR-001 的 10 秒仍達不到，且沒有現成解法。** 第一段 p90 = 10310ms（73% 落在
     10 秒內，判準要 90%）。換模型排除了，`user_id` 只值 54ms。
     **plan MUST 在「放寬時限」與「其他設計手段」之間做出選擇並說明理由。**
   - 💡 **兩段可掛不同 agent**（agent id 由 env 注入），仍是可行取捨項。

   ⚠️ **兩個方法論教訓，都已寫進 §8.2b**：
   ① 延遲比較 **MUST NOT 以 n=5 下結論**——這次有三個結論被 n=15 推翻。
   ② 量測工具 **MUST 共用正式路徑的程式碼**——腳本自抄的 JSON 抽取漏了一步，
   把正常的摘要 agent 判成 0/15 不合規，而它的註解還寫著「比照正式路徑」。

2. **004 走完 plan → tasks → implement。**
   004 的設計相依（FR-006／FR-011／FR-013／FR-014 踩在 003 的併發去重與失敗批次記憶上）
   已隨 003 落地解除，**現在可以動**。相依是單向的：003 不依賴 004。

3. **002 的 SC-001 計時**與 004 自己的人工驗收留到最後。

4. 通過後才押 **`m2-004-done`**（002／003／004 共押一個，中間不各自押，這是刻意的）。
   ⚠️ **annotation MUST 反映「US2 曾經完全不可用、2026-08-27 才修好」** ——
   002 曾押的 `m2-002-done` 已依使用者決定刪除（未曾 push），那段歷史沒有別的落點。
   ⚠️ tag 一旦建立就不移動；「落後 HEAD」是它的正常狀態，也正是它的用途。

---

## 給接手者的一句話

**自動化全綠不等於功能存在。** 2026-08-28 又證明了一次：在 304 項測試全過、smoke 兩支全綠
之後，真實環境的人工驗收仍挖出四個新缺陷，其中三個同樣不報錯。
剩下的 US2-C 卡在「需要第二個帳號」，那不是靠寫程式能繞過的。
