# 交接筆記 —— 2026-08-27 深夜／28 凌晨

> ⚠️ **這是一份有時效的快照**，跨越 002／003／004 三份規格。內容一旦被消化（明天的工作做完），
> 就該刪掉，不要讓它變成第四個描述同一件事的地方（CLAUDE.md 的核心警告）。
> 正典結論都已寫進各自的 spec、`docs/ARCHITECTURE.md` 與 `docs/IMBRACE_QUESTIONS.md`，這裡只放
> 「明天要接著做什麼」與「別再踩一次的坑」。

## 現在在哪裡

`feat/m2-copilot-panel`，worktree 乾淨，未 push。最近四個 commit：

| commit | 內容 |
|---|---|
| `29ad02d` | `fix(002)` 知識庫 prompt 措辭 ＋ 逾時分離（快查 30s／建議卡 8s） |
| `9ad1737` | `docs(002)` SC-002 由 10 秒改寫為 25 秒 ＋ 七處舊說法同步 |
| `0cc0fba` | `docs(003)` 分析管線的觸發與失敗政策（specify 完成） |
| `7f88dd4` | `docs(004)` 建議卡的漸進式知識庫引用（specify 完成） |

003 與 004 都**只完成 specify**，checklist 全過、無待答項目，尚未跑 plan。

## 明天的順序（已與使用者議定）

1. **輕量人工驗收（15–20 分鐘）** —— 見下一節。**不要跳過。**
2. **003 走完 plan → tasks → implement**
3. **004 走完 plan → tasks → implement**
4. **完整人工驗收**（SC-001 計時、US2 六個檢查點、US4 多對話），通過後才押 `m2-002-done`

為什麼完整驗收排在最後：003 會改 `attach()` 的觸發時機與 LEAVE 行為（US4 與斷線重連走的就是那條路徑），
004 會改建議卡的產出時序（US1 的 SC-001 計時會被影響）。先做完整驗收等於驗一份即將被改掉的程式碼。

為什麼第 1 步不能省：**US2 知識庫快查壞了不知道多久，自動化測試全綠、沒有人發現** ——
測試驗的是「解析邏輯正確」，不是「agent 真的會檢索」。它昨晚才剛修好，而且**只有腳本驗過，
沒有人用真的介面看過一眼**。在這個狀態上再疊兩份大改動，等於重演同一個錯誤。

## 第 1 步：輕量驗收的三個檢查點

```bash
npm run dev     # http://localhost:3000
```

啟動後**先看終端機**，確認**沒有**這兩行（有的話代表跑的是假資料，整輪白跑）：

```
[ai] 缺少 ... 退回 MockAIProvider
[knowledge] 缺少 ... 退回 MockKnowledgeProvider
```

登入 → 挑一個**內部測試對話** → 加入對話，只確認三件事**能用**（不計時、不跑完整場景）：

1. **知識庫快查**：輸入一句自然語言查詢 → 骨架 → 出現結果列表。⚠️ **會等 13～25 秒，那是正常的**，
   不要因為慢就判定失敗（實測值，已寫進 quickstart 已知限制）。
2. **建議卡**：出得來、有語氣標籤、`confidence` **留空不顯示**（看到百分比數字就是 bug）。
   ⚠️ 卡片會標「未引用知識庫」—— **那是預期行為**，正是 004 要解的問題，不是缺陷。
3. **一鍵帶入**：文字進得了 Composer、可編輯、不自動送出。

⚠️ `IMBRACE_ENV=stable` 是**正式環境**：JOIN、送訊息、切 mode 都是真的寫入真實客戶對話。

## 未解的線頭

- **`m2-002-done` 尚未押。** 昨晚曾押在 `73ad669` 後依使用者決定刪除（未曾 push，遠端無殘留）。
  目標 commit 待完整驗收後才確定。另一個 session（`agent-copilot-e9`）在等結案敘述，
  屆時把 hash 與敘述給它即可。⚠️ 新的 annotation 要能反映「US2 曾經完全不可用、8/27 才修好」
  這件事，不能只寫「已完成 specify→implement 循環」。
- **SC-001（3 秒／10 秒真實環境計時）從未執行過**，見 002 `tasks.md` T069。
- **知識庫 agent 的模型還沒定案。** 目前停在 `qwen.qwen3-32b-v1:0`（實測 20.5／13.0／18.6 秒、
  3 筆命中，可用）。`us.amazon.nova-pro-v1:0` 實測相當（13.1／16.7／17.1 秒）且多一個獨立對照樣本。
  兩者皆可，使用者尚未選定。⚠️ **`google.gemma-3-27b-it` 不可用** —— 無原生 function calling，
  不論 prompt 怎麼寫都只會把工具呼叫印成文字。
- **診斷腳本是拋棄式的**，寫在 session scratchpad、**不在 repo 裡**，明天就沒了。若之後還要重測
  檢索（例如 004 落地後），值得把它收成 `scripts/spike/18-knowledge-latency.ts`＋`npm run spike:knowledge-latency`。

## 昨晚踩到、別再踩第二次的坑

1. **agent 模型不支援 function calling 時，一切都「正常」** —— 不報錯、型別正確，只是永遠 0 命中。
   判斷方式：看 SSE 有沒有 `tool-output-available` 事件，沒有就是根本沒呼叫。
2. **prompt 措辭會決定模型「執行」還是「敘述」工具呼叫。** 命令式（「請在知識庫中搜尋…段落」）
   會讓模型印出 ```` ```tool_code ```` 文字；自然提問才會真的呼叫。措辭已寫死在
   `buildKnowledgePrompt()` 並附「改動前必須重測」的警語。
3. **測試依賴 gitignore 的檔案 = 定時炸彈。** `test/agent-knowledge-provider.test.ts` 原本讀
   `scripts/spike/out/` 裡的 fixture，那個目錄在 `.gitignore` 裡，檔案一沒就必紅。已改為內嵌樣本。
4. **文件同步要 grep 到第三輪。** SC-002 的「10 秒」散在七個地方，`data-model.md` 那份鏡像是
   第三輪掃描才抓到的。
5. **smoke 紅了先確認前提是否成立**，不要急著放寬門檻 —— 場景⑤的 FR-020 斷言連 4 次全紅，
   根因是測試自己沒讓 B 離開對話，而優先度是整個對話聚合的。動前置條件，不動斷言。

## 給明天接手者的一句話

如果只讀一行：**先花 20 分鐘用真的介面確認知識庫快查與建議卡能用，再開始寫 003 的 plan。**
昨晚整晚的教訓就是「自動化全綠不等於功能存在」。
