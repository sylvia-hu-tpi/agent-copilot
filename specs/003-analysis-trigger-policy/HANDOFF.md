# 交接筆記 —— M2 收尾（002／003／004）

> ⚠️ **這是一份有時效的滾動快照**，跨越 002／003／004 三份規格。做完一段就把該段刪掉，
> 不要讓它變成第四個描述同一件事的地方（CLAUDE.md 的核心警告）。
> 正典結論都已寫進各自的 spec、`docs/ARCHITECTURE.md` 與 `docs/IMBRACE_QUESTIONS.md`，這裡只放
> 「接著要做什麼」與「別再踩一次的坑」。
>
> **最後更新：2026-08-28**（輕量驗收已完成、003 已跑完 plan → tasks → analyze）

## 現在在哪裡

`feat/m2-copilot-panel`，未 push。

| 規格 | 狀態 |
|---|---|
| 001 | implement 完成，`m2-001-done` 已押 |
| 002 | implement 完成（32 項全打勾）。**完整人工驗收未做**，SC-001 真實環境計時從未執行（見「未解的線頭」） |
| 003 | spec → plan → tasks → analyze 完成，analyze 的 16 條 findings 已修畢。**55 項任務全未開始** |
| 004 | 只有 spec + checklists，**plan 未跑** |

## 接下來的順序（已與使用者議定）

1. ~~輕量人工驗收（15–20 分鐘）~~ ✅ **2026-08-28 已完成** —— 知識庫快查、建議卡、一鍵帶入三項皆可用
2. **003 走完 implement**（plan／tasks／analyze 已完成）
3. **004 走完 plan → tasks → implement**
4. **完整人工驗收**（SC-001 計時、US2 六個檢查點、US4 多對話），通過後才押 `m2-004-done`

**為什麼 004 的 plan 必須等 003 落地**（比原本寫的「驗收順序」更硬 —— 這是**設計相依**）：
004 的 FR-006（第二段不得覆蓋更新的結果）、FR-011（不得繞過既有去重）、FR-014（AI 呼叫上限 2 次）
全都踩在 003 才要建立的同區塊併發去重與失敗批次記憶上，FR-013 的背景優先度判定則走 003 T016
要改的 `watch()`。在 003 之前寫 004 的 plan，等於把 plan 建立在一個**還不存在的併發模型**上，
003 落地後整份要重寫。反過來 003 不依賴 004，相依是單向的。

**為什麼完整驗收排在最後**：003 會改 `attach()` 的觸發時機與 LEAVE 行為（US4 與斷線重連走的就是
那條路徑），004 會改建議卡的產出時序（US1 的 SC-001 計時會被影響）。先做完整驗收等於驗一份
即將被改掉的程式碼。

## 未解的線頭

- **`m2-002-done` 不再單押，改為在 004 收尾時押 `m2-004-done`。** 002 曾押在 `73ad669` 後依使用者
  決定刪除（未曾 push，遠端無殘留）。既然 002／003／004 連著跑、完整驗收只做一次，就押一個
  涵蓋整段的 tag —— tag 序列會是 `m2-001-done` → `m2-004-done`，中間不各自押，這是刻意的。
  ⚠️ **但原本要寫進 `m2-002-done` 的那段歷史沒有落點了**：`m2-004-done` 的 annotation MUST 反映
  「US2 曾經完全不可用、8/27 才修好」這件事，不能只寫「已完成 specify→implement 循環」。
  另一個 session（`agent-copilot-e9`）在等結案敘述，屆時把 hash 與敘述給它即可。
- **SC-001（3 秒／10 秒真實環境計時）從未執行過**，見 002 `tasks.md` T069。
- **知識庫 agent 的模型還沒定案。** 目前停在 `qwen.qwen3-32b-v1:0`（實測 20.5／13.0／18.6 秒、
  3 筆命中，可用）。`us.amazon.nova-pro-v1:0` 實測相當（13.1／16.7／17.1 秒）且多一個獨立對照樣本。
  兩者皆可，使用者尚未選定。⚠️ **`google.gemma-3-27b-it` 不可用** —— 無原生 function calling，
  不論 prompt 怎麼寫都只會把工具呼叫印成文字。
- **診斷腳本是拋棄式的**，寫在 session scratchpad、**不在 repo 裡**，明天就沒了。若之後還要重測
  檢索（例如 004 落地後），值得把它收成 `scripts/spike/18-knowledge-latency.ts`＋`npm run spike:knowledge-latency`。

## 2026-08-27 深夜踩到、別再踩第二次的坑

1. **agent 模型不支援 function calling 時，一切都「正常」** —— 不報錯、型別正確，只是永遠 0 命中。
   判斷方式：看 SSE 有沒有 `tool-output-available` 事件，沒有就是根本沒呼叫。
2. **prompt 措辭會決定模型「執行」還是「敘述」工具呼叫。** 命令式（「請在知識庫中搜尋…段落」）
   會讓模型印出 ```` ```tool_code ```` 文字；自然提問才會真的呼叫。措辭已寫死在
   `buildKnowledgePrompt()` 並附「改動前必須重測」的警語。
3. **測試依賴 gitignore 的檔案 = 定時炸彈。** `test/agent-knowledge-provider.test.ts` 原本讀
   `scripts/spike/out/` 裡的 fixture，那個目錄在 `.gitignore` 裡，檔案一沒就必紅。已改為內嵌樣本。
4. **文件同步要 grep 到第三輪。** SC-002 的「10 秒」散在七個地方，`data-model.md` 那份鏡像是
   第三輪掃描才抓到的。
5. **smoke 紅了先確認前提是否成立**，不要急著放寬門檻 —— 場景⑤的 FR-020 斷言連 4 次全紅，
   根因是測試自己沒讓 B 離開對話，而優先度是整個對話聚合的。動前置條件，不動斷言。

## 給接手者的一句話

如果只讀一行：**003 的三個缺陷都不會報錯，測試全綠不代表修好了** ——
`tasks.md` T052 的人工驗收不可省略。這與 8/27 那晚的教訓是同一句話的兩個版本：
自動化全綠不等於功能存在，也不等於缺陷已消失。
